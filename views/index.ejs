<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Template Sites | Robyn Veitch</title>
    <link rel="stylesheet" href="/resources/css/index.css" />
    <link rel="icon" type="image/png" href="/faveicon.png">
  </head>
  <body>
    <h1 class="main__title">Robyn Veitch | Template Sites</h1>
    <h2 class="main__description">Shown here are a series of sites, apps and single web pages to be ‘real’ websites and layout templates, whilst the <a href="https://oddert.github.io/#code_anchor">other projects</a> focus on a particular function and not necessarily a full user facing experience.</h2>
    <div class="sites">
      <% sites.forEach(each => { %>
        <div class="site <%= each.classInserts ? each.classInserts : 'small' %>" title="<%= each.title %>" data-id="<%= each._id %>">
          <!-- <a href="<%= each.local ? each.onsiteURL : each.offsiteURL %>"> -->
          <a>
            <figure class="site_background">
              <img src="<%= each.thumbnail %>" alt="<%= each.title %> banner image thumbnail.">
            </figure>
            <div class="site_thumb">
            <img src="<%= each.thumbnail %>" alt="<%= each.title %> banner image thumbnail.">
          </div>
            <div class="site__details">
              <h3 class="site__title">
                <span><%= each.title %></span>
              </h3>
              <p class="site__subtitle"><%= each.subtitle %></p>
            </div>
          </a>
        </div>
      <% }) %>
    </div>
    <div class="details_window__container no_show">
      <div class="details_window">
        <button class="details_window--close">✖</button>
        <h2 class="details_window__title">No title Selected</h2>
        <h3 class="details_window__subtitle">No Description yet</h3>
        <p class="details_window__description">No Description yet</p>
        <img class="details_window__image" src="" alt="" />
      </div>
    </div>
  </body>
  <script type="text/javascript" src="/resources/js/index.js"></script>
</html>
